# SvgBadgeGenerator
Takes in a .csv file and generates .svg(s) from it, and also puts them all in one file.

# Setup
- have a folder named `csh50th_cards` in your computer's `Downloads` folder. The program will not work otherwise
- run `main.py`
